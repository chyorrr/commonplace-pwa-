import {
  CodedError,
  PermissionStatus,
  UnavailabilityError,
  createPermissionHook
} from "./chunk-JDHVKVZV.js";
import "./chunk-T6H7DVFP.js";
import "./chunk-4NIFX77G.js";
import "./chunk-J7KF3FXP.js";
import "./chunk-UXOEEXWM.js";
import "./chunk-D57FLT73.js";

// node_modules/expo-image-picker/src/ImagePicker.types.ts
var MediaTypeOptions = /* @__PURE__ */ ((MediaTypeOptions2) => {
  MediaTypeOptions2["All"] = "All";
  MediaTypeOptions2["Videos"] = "Videos";
  MediaTypeOptions2["Images"] = "Images";
  return MediaTypeOptions2;
})(MediaTypeOptions || {});
var VideoExportPreset = /* @__PURE__ */ ((VideoExportPreset2) => {
  VideoExportPreset2[VideoExportPreset2["Passthrough"] = 0] = "Passthrough";
  VideoExportPreset2[VideoExportPreset2["LowQuality"] = 1] = "LowQuality";
  VideoExportPreset2[VideoExportPreset2["MediumQuality"] = 2] = "MediumQuality";
  VideoExportPreset2[VideoExportPreset2["HighestQuality"] = 3] = "HighestQuality";
  VideoExportPreset2[VideoExportPreset2["H264_640x480"] = 4] = "H264_640x480";
  VideoExportPreset2[VideoExportPreset2["H264_960x540"] = 5] = "H264_960x540";
  VideoExportPreset2[VideoExportPreset2["H264_1280x720"] = 6] = "H264_1280x720";
  VideoExportPreset2[VideoExportPreset2["H264_1920x1080"] = 7] = "H264_1920x1080";
  VideoExportPreset2[VideoExportPreset2["H264_3840x2160"] = 8] = "H264_3840x2160";
  VideoExportPreset2[VideoExportPreset2["HEVC_1920x1080"] = 9] = "HEVC_1920x1080";
  VideoExportPreset2[VideoExportPreset2["HEVC_3840x2160"] = 10] = "HEVC_3840x2160";
  return VideoExportPreset2;
})(VideoExportPreset || {});
var UIImagePickerControllerQualityType = /* @__PURE__ */ ((UIImagePickerControllerQualityType2) => {
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["High"] = 0] = "High";
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["Medium"] = 1] = "Medium";
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["Low"] = 2] = "Low";
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["VGA640x480"] = 3] = "VGA640x480";
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["IFrame1280x720"] = 4] = "IFrame1280x720";
  UIImagePickerControllerQualityType2[UIImagePickerControllerQualityType2["IFrame960x540"] = 5] = "IFrame960x540";
  return UIImagePickerControllerQualityType2;
})(UIImagePickerControllerQualityType || {});
var UIImagePickerPresentationStyle = /* @__PURE__ */ ((UIImagePickerPresentationStyle2) => {
  UIImagePickerPresentationStyle2["FULL_SCREEN"] = "fullScreen";
  UIImagePickerPresentationStyle2["PAGE_SHEET"] = "pageSheet";
  UIImagePickerPresentationStyle2["FORM_SHEET"] = "formSheet";
  UIImagePickerPresentationStyle2["CURRENT_CONTEXT"] = "currentContext";
  UIImagePickerPresentationStyle2["OVER_FULL_SCREEN"] = "overFullScreen";
  UIImagePickerPresentationStyle2["OVER_CURRENT_CONTEXT"] = "overCurrentContext";
  UIImagePickerPresentationStyle2["POPOVER"] = "popover";
  UIImagePickerPresentationStyle2["AUTOMATIC"] = "automatic";
  return UIImagePickerPresentationStyle2;
})(UIImagePickerPresentationStyle || {});
var UIImagePickerPreferredAssetRepresentationMode = /* @__PURE__ */ ((UIImagePickerPreferredAssetRepresentationMode2) => {
  UIImagePickerPreferredAssetRepresentationMode2["Automatic"] = "automatic";
  UIImagePickerPreferredAssetRepresentationMode2["Compatible"] = "compatible";
  UIImagePickerPreferredAssetRepresentationMode2["Current"] = "current";
  return UIImagePickerPreferredAssetRepresentationMode2;
})(UIImagePickerPreferredAssetRepresentationMode || {});
var CameraType = /* @__PURE__ */ ((CameraType2) => {
  CameraType2["back"] = "back";
  CameraType2["front"] = "front";
  return CameraType2;
})(CameraType || {});

// node_modules/expo-image-picker/src/utils.ts
function parseMediaTypes(mediaTypes) {
  const mediaTypeOptionsToMediaType = {
    Images: ["images"],
    Videos: ["videos"],
    All: ["images", "videos"]
  };
  if (mediaTypes === "Images" /* Images */ || mediaTypes === "Videos" /* Videos */ || mediaTypes === "All" /* All */) {
    console.warn(
      "[expo-image-picker] `ImagePicker.MediaTypeOptions` have been deprecated. Use `ImagePicker.MediaType` or an array of `ImagePicker.MediaType` instead."
    );
    return mediaTypeOptionsToMediaType[mediaTypes];
  }
  if (typeof mediaTypes === "string") {
    return [mediaTypes];
  }
  return mediaTypes;
}
function mapDeprecatedOptions(options) {
  if (!options.mediaTypes) {
    return options;
  }
  return { ...options, mediaTypes: parseMediaTypes(options.mediaTypes ?? []) };
}

// node_modules/expo-image-picker/src/ExponentImagePicker.web.ts
var MediaTypeInput = {
  images: "image/*",
  videos: "video/mp4,video/quicktime,video/x-m4v,video/*",
  livePhotos: ""
};
var ExponentImagePicker_web_default = {
  async launchImageLibraryAsync({
    mediaTypes = ["images"],
    allowsMultipleSelection = false,
    base64 = false
  }) {
    if (typeof window === "undefined" || typeof document === "undefined") {
      return { canceled: true, assets: null };
    }
    return await openFileBrowserAsync({
      mediaTypes,
      allowsMultipleSelection,
      base64
    });
  },
  async launchCameraAsync({
    mediaTypes = ["images"],
    allowsMultipleSelection = false,
    base64 = false,
    cameraType
  }) {
    if (typeof window === "undefined" || typeof document === "undefined") {
      return { canceled: true, assets: null };
    }
    return await openFileBrowserAsync({
      mediaTypes,
      allowsMultipleSelection,
      capture: cameraType ?? true,
      base64
    });
  },
  /*
   * Delegate to expo-permissions to request camera permissions
   */
  async getCameraPermissionsAsync() {
    return permissionGrantedResponse();
  },
  async requestCameraPermissionsAsync() {
    return permissionGrantedResponse();
  },
  /*
   * Camera roll permissions don't need to be requested on web, so we always
   * respond with granted.
   */
  async getMediaLibraryPermissionsAsync(_writeOnly) {
    return permissionGrantedResponse();
  },
  async requestMediaLibraryPermissionsAsync(_writeOnly) {
    return permissionGrantedResponse();
  }
};
function permissionGrantedResponse() {
  return {
    status: "granted" /* GRANTED */,
    expires: "never",
    granted: true,
    canAskAgain: true
  };
}
function openFileBrowserAsync({
  mediaTypes,
  capture = false,
  allowsMultipleSelection = false,
  base64
}) {
  const parsedMediaTypes = parseMediaTypes(mediaTypes);
  const mediaTypeFormat = createMediaTypeFormat(parsedMediaTypes);
  const input = document.createElement("input");
  input.style.display = "none";
  input.setAttribute("type", "file");
  input.setAttribute("accept", mediaTypeFormat);
  input.setAttribute("id", String(Math.random()));
  input.setAttribute("data-testid", "file-input");
  if (allowsMultipleSelection) {
    input.setAttribute("multiple", "multiple");
  }
  if (capture) {
    switch (capture) {
      case true:
        input.setAttribute("capture", "camera");
        break;
      case "front" /* front */:
        input.setAttribute("capture", "user");
        break;
      case "back" /* back */:
        input.setAttribute("capture", "environment");
    }
  }
  document.body.appendChild(input);
  return new Promise((resolve) => {
    input.addEventListener("change", async () => {
      var _a;
      if ((_a = input.files) == null ? void 0 : _a.length) {
        const files = allowsMultipleSelection ? input.files : [input.files[0]];
        try {
          const assets = await Promise.all(
            Array.from(files).filter((file) => file != null).map((file) => readFile(file, { base64 }))
          );
          resolve({ canceled: false, assets });
        } catch (error) {
          resolve(Promise.reject(error));
        }
      } else {
        resolve({ canceled: true, assets: null });
      }
      document.body.removeChild(input);
    });
    input.addEventListener("cancel", () => {
      input.dispatchEvent(new Event("change"));
    });
    const event = new MouseEvent("click");
    input.dispatchEvent(event);
  });
}
async function getImageMetadata(blobUrl) {
  return new Promise((resolve) => {
    const image = new Image();
    image.onload = () => {
      resolve({
        width: image.naturalWidth ?? image.width,
        height: image.naturalHeight ?? image.height
      });
    };
    image.onerror = () => resolve({ width: 0, height: 0 });
    image.src = blobUrl;
  });
}
async function getVideoMetadata(blobUrl) {
  return new Promise((resolve) => {
    const video = document.createElement("video");
    video.preload = "metadata";
    video.onloadedmetadata = () => {
      resolve({
        width: video.videoWidth,
        height: video.videoHeight,
        duration: video.duration
      });
    };
    video.onerror = () => resolve({ width: 0, height: 0, duration: 0 });
    video.src = blobUrl;
  });
}
async function readFileAsBase64(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = () => {
      reject(new Error("Failed to read the selected media because the operation failed."));
    };
    reader.onload = (event) => {
      var _a, _b;
      const result = (_a = event.target) == null ? void 0 : _a.result;
      if (typeof result !== "string") {
        reject(new Error("Failed to read file as base64"));
        return;
      }
      resolve(((_b = result.split(",")) == null ? void 0 : _b[1]) ?? "");
    };
    reader.readAsDataURL(file);
  });
}
async function readFile(targetFile, options) {
  const mimeType = targetFile.type;
  const baseUri = URL.createObjectURL(targetFile);
  try {
    let metadata;
    let base64;
    if (mimeType.startsWith("image/")) {
      metadata = await getImageMetadata(baseUri);
    } else if (mimeType.startsWith("video/")) {
      metadata = await getVideoMetadata(baseUri);
    } else {
      throw new Error(`Unsupported file type: ${mimeType}. Only images and videos are supported.`);
    }
    if (options.base64) {
      base64 = await readFileAsBase64(targetFile);
    }
    return {
      uri: baseUri,
      width: metadata.width,
      height: metadata.height,
      type: mimeType.startsWith("image/") ? "image" : "video",
      mimeType,
      fileName: targetFile.name,
      fileSize: targetFile.size,
      file: targetFile,
      ...metadata.duration !== void 0 && { duration: metadata.duration },
      ...base64 && { base64 }
    };
  } catch (error) {
    throw error;
  }
}
function createMediaTypeFormat(mediaTypes) {
  const filteredMediaTypes = mediaTypes.filter((mediaType) => mediaType !== "livePhotos");
  if (filteredMediaTypes.length === 0) {
    return "image/*";
  }
  let result = "";
  for (const mediaType of filteredMediaTypes) {
    if (!result.includes(MediaTypeInput[mediaType])) {
      result = result.concat(",", MediaTypeInput[mediaType]);
    }
  }
  return result;
}

// node_modules/expo-image-picker/src/ImagePicker.ts
function validateOptions(options) {
  const { aspect, quality, videoMaxDuration } = options;
  if (aspect != null) {
    const [x, y] = aspect;
    if (x <= 0 || y <= 0) {
      throw new CodedError(
        "ERR_INVALID_ARGUMENT",
        `Invalid aspect ratio values ${x}:${y}. Provide positive numbers.`
      );
    }
  }
  if (quality && (quality < 0 || quality > 1)) {
    throw new CodedError(
      "ERR_INVALID_ARGUMENT",
      `Invalid 'quality' value ${quality}. Provide a value between 0 and 1.`
    );
  }
  if (videoMaxDuration && videoMaxDuration < 0) {
    throw new CodedError(
      "ERR_INVALID_ARGUMENT",
      `Invalid 'videoMaxDuration' value ${videoMaxDuration}. Provide a non-negative number.`
    );
  }
  return options;
}
async function getCameraPermissionsAsync() {
  return ExponentImagePicker_web_default.getCameraPermissionsAsync();
}
async function getMediaLibraryPermissionsAsync(writeOnly = false) {
  return ExponentImagePicker_web_default.getMediaLibraryPermissionsAsync(writeOnly);
}
async function requestCameraPermissionsAsync() {
  return ExponentImagePicker_web_default.requestCameraPermissionsAsync();
}
async function requestMediaLibraryPermissionsAsync(writeOnly = false) {
  const imagePickerMethod = ExponentImagePicker_web_default.requestMediaLibraryPermissionsAsync;
  return imagePickerMethod(writeOnly);
}
var useMediaLibraryPermissions = createPermissionHook({
  // TODO(cedric): permission requesters should have an options param or a different requester
  getMethod: (options) => getMediaLibraryPermissionsAsync(options == null ? void 0 : options.writeOnly),
  requestMethod: (options) => requestMediaLibraryPermissionsAsync(options == null ? void 0 : options.writeOnly)
});
var useCameraPermissions = createPermissionHook({
  getMethod: getCameraPermissionsAsync,
  requestMethod: requestCameraPermissionsAsync
});
async function getPendingResultAsync() {
  if (ExponentImagePicker_web_default.getPendingResultAsync) {
    return ExponentImagePicker_web_default.getPendingResultAsync();
  }
  return null;
}
async function launchCameraAsync(options = {}) {
  if (!ExponentImagePicker_web_default.launchCameraAsync) {
    throw new UnavailabilityError("ImagePicker", "launchCameraAsync");
  }
  const mappedOptions = mapDeprecatedOptions(options);
  return await ExponentImagePicker_web_default.launchCameraAsync(validateOptions(mappedOptions));
}
async function launchImageLibraryAsync(options = {}) {
  const mappedOptions = mapDeprecatedOptions(options);
  if (!ExponentImagePicker_web_default.launchImageLibraryAsync) {
    throw new UnavailabilityError("ImagePicker", "launchImageLibraryAsync");
  }
  if ((mappedOptions == null ? void 0 : mappedOptions.allowsEditing) && mappedOptions.allowsMultipleSelection) {
    console.warn(
      "[expo-image-picker] `allowsEditing` is not supported when `allowsMultipleSelection` is enabled and will be ignored.Disable either 'allowsEditing' or 'allowsMultipleSelection' in 'launchImageLibraryAsync' to fix this warning."
    );
  }
  return await ExponentImagePicker_web_default.launchImageLibraryAsync(mappedOptions);
}
export {
  CameraType,
  MediaTypeOptions,
  PermissionStatus,
  UIImagePickerControllerQualityType,
  UIImagePickerPreferredAssetRepresentationMode,
  UIImagePickerPresentationStyle,
  VideoExportPreset,
  getCameraPermissionsAsync,
  getMediaLibraryPermissionsAsync,
  getPendingResultAsync,
  launchCameraAsync,
  launchImageLibraryAsync,
  requestCameraPermissionsAsync,
  requestMediaLibraryPermissionsAsync,
  useCameraPermissions,
  useMediaLibraryPermissions
};
//# sourceMappingURL=expo-image-picker.js.map
