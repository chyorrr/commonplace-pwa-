import { Board, CustomSticker, DeskItem, MemorySnippet } from '../types';

export const initialStickers: CustomSticker[] = [];

export const initialBoards: Board[] = [];

export const initialDeskItems: DeskItem[] = [];

export const initialMemory: MemorySnippet | null = null;
